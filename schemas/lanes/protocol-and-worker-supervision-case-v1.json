{
  "schema_version": "turnvector.benchmark.case-schema.v1",
  "lane_id": "protocol-and-worker-supervision",
  "matrices": {
    "worker-outcome": {
      "required": ["outcome", "protocol_relation"],
      "properties": {
        "outcome": {"enum": ["normal", "crash_before_start", "crash_during_turn", "timeout", "malformed_frame", "duplicate_receipt"]},
        "protocol_relation": {"enum": ["exact", "compatible", "incompatible", "unknown_capability"]}
      },
      "additional_properties": false
    }
  }
}
